name: Release CI

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Release workflow smoke test
        run: echo "OpenRealm Release CI is valid"
