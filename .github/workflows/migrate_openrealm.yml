name: OpenRealm Migration

on:
  push:
    branches:
      - openrealm-init
  workflow_dispatch:

permissions:
  contents: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Smoke test
        run: |
          echo "OpenRealm migration runner is active"
          git status --short
